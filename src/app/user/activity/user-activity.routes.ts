import { Routes } from '@angular/router';
import { UserActivityComponent } from './user-activity.component';

export const UserActivityRoutes: Routes = [{ path: '', component: UserActivityComponent }];

